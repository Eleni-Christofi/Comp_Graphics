
//const Sphere light(Vector(0, 0, 50), 1);
//Vector L = light.c - hit.position;
//L = L.normalise();
//Vector N = hit.normal;
//N = N.normalise();
//float dt = L.dot(N);
//hit.colour = white * dt;
//clamp255(hit.colour);